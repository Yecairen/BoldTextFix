package dev.yecairen.boldtextfix;

/** Stores source details that cannot be derived from a text style alone. */
public interface CustomGlyph {
    boolean boldtextfix$isTrueType();

    void boldtextfix$setTrueType(boolean trueType);
}
